$('#<%=@user.id%>name').html("<%= j (render partial: 'edit_name') %>");
$('#<%=@user.id%>email').html("<%= j (render partial: 'edit_email') %>");
$('#<%=@user.id%>phone').html("<%= j (render partial: 'edit_phone') %>");
$('#<%=@user.id%>image').html("<%= j (render partial: 'edit_image') %>");
